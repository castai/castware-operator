{{- if .Values.defaultComponents.enabled }}
{{- if .Values.defaultComponents.umbrella.enabled }}
{{- /* Compute the effective umbrella mode tags: an explicit tags block wins;
       otherwise extendedPermissions derives tags.full (everything) and the
       default is tags.readonly (agent, spot-handler, kvisor only). */ -}}
{{- $umbrellaTags := .Values.defaultComponents.umbrella.tags }}
{{- if not $umbrellaTags }}
{{-   if .Values.extendedPermissions }}
{{-     $umbrellaTags = dict "full" true }}
{{-   else }}
{{-     $umbrellaTags = dict "readonly" true }}
{{-   end }}
{{- end }}
---
apiVersion: castware.cast.ai/v1alpha1
kind: Component
metadata:
  name: {{ .Values.defaultComponents.umbrella.component | default "castai-umbrella" }}
  namespace: {{ .Release.Namespace }}
  annotations:
    helm.sh/hook: post-install
    helm.sh/hook-delete-policy: hook-failed
    helm.sh/hook-weight: "10"
  labels:
    {{- include "castware-operator.labels" . | nindent 4 }}
spec:
  component: {{ .Values.defaultComponents.umbrella.component | default "castai-umbrella" | quote }}
  cluster: {{ .Values.defaultComponents.umbrella.cluster | default "castai" | quote }}
  enabled: true
{{- /* Build the umbrella CR values in one map so overrides merge instead of
       emitting duplicate keys (e.g. two autoscaler blocks would drop one). */}}
{{- $umbrellaValues := mergeOverwrite (dict "tags" $umbrellaTags "autoscaler" (dict "castai-live" (dict "enabled" (.Values.defaultComponents.umbrella.live.enabled)))) (.Values.defaultComponents.umbrella.overrides | default dict) }}
  values:
    {{- toYaml $umbrellaValues | nindent 4 }}
{{- else }}
{{- range $name, $comp := .Values.defaultComponents.components }}
---
apiVersion: castware.cast.ai/v1alpha1
kind: Component
metadata:
  name: {{ $name }}
  namespace: {{ $.Release.Namespace }}
  annotations:
    helm.sh/hook: post-install
    helm.sh/hook-delete-policy: hook-failed
    helm.sh/hook-weight: "10"
  labels:
    {{- include "castware-operator.labels" $ | nindent 4 }}
spec:
  component: {{ $comp.component | quote }}
  cluster: {{ $comp.cluster | quote }}
  enabled: {{ $comp.enabled }}
  {{- if $comp.version }}
  version: {{ $comp.version | quote }}
  {{- end }}
  {{- if $comp.overrides }}
  values:
    {{- toYaml $comp.overrides | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}
{{- end }}
