{{- if .Values.crdUpgrade.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "castware-operator.fullname" . }}-crds
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "castware-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
data:
  crds.yaml: |
{{ .Files.Get "crds/crds.yaml" | indent 4 }}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "castware-operator.fullname" . }}-crd-upgrade-{{ .Release.Revision }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "castware-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "0"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  backoffLimit: 3
  template:
    metadata:
      name: {{ include "castware-operator.fullname" . }}-crd-upgrade
      labels:
        {{- include "castware-operator.labels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      restartPolicy: Never
      serviceAccountName: {{ include "castware-operator.fullname" . }}-controller-manager
      containers:
      - name: crd-upgrade
        image: {{ .Values.crdUpgrade.image.repository }}:{{ .Values.crdUpgrade.image.tag }}
        imagePullPolicy: {{ .Values.crdUpgrade.image.pullPolicy }}
        command:
        - /bin/sh
        - -c
        - |
          set -e
          echo "Applying CRDs..."
          kubectl apply -f /crds/crds.yaml
          echo "CRDs applied successfully"
        volumeMounts:
        - name: crds
          mountPath: /crds
          readOnly: true
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          runAsUser: 65532
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
          seccompProfile:
            type: RuntimeDefault
      volumes:
      - name: crds
        configMap:
          name: {{ include "castware-operator.fullname" . }}-crds
{{- end }}
